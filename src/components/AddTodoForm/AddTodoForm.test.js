import React from "react"

describe("AddTodoForm", () => {

  const sampleId = 1

  const sampleTodo = {
    id: sampleId,
    completed: false,
    priority: true,
    name: "Do this thing"
  }

  it("should add a todo with enter", () => {
  })

  it("should add task with button", () => {
  })

  it("should fail to add task without a name", () => {
  })

})
